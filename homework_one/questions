1) git system that tracks file changes it benedites users by makeing possable to revert changes and also makeing possable to work simultaneously with other people safely

2) vsc is git tool that makes possable to revert changes in files when somthing goes wrong

3) repository or repo is folder that contains all files and changes made to project also it has two forms local and cloud local is one that stores on your system and cloud is that is stored on github.

4) commit is snapshot of project or file at the one pont of time. when someone comits the project it saves the project curent state.

5) git add tells git to include content in commit and comit actually saves project.

6) git push pushes project online and git pull downloads the project from the page.

7) branch is a separate part of project and it makes so that developer can work on other fitures without touching the main one.



8) github is a platform where people can upload the projects but git is the tool

9) gitlabs and all-in-one devops

10) everybody can see public repo and only the creator and people who creator invited can see rpivate repo.

11) readme.md is file tjat is used by developers to explain what is the project for.

12) gitignore is a flie that tells git what file not to comit. developers use it so database api keys passwords doesn`t leak.