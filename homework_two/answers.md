1) prompt generates window in console and user can interact and put text in there. it returns string.

2) it is '42' because it returns strings only.

3) to make them numbers.

4) typeof tells us what datatype is a value for example we can use it for knowing what datatype is a defined value.

5) touppercase makes al the characters uppercase same as tolowercase it makes all the charactesrs lowercase and trim makes that all the spaces in the string desapier.

6) \n make so the string is printed on another line in console and \t places tab where it is.

7) string is a text we cant use operators like + with it for mathematical porpuses with + we can combine two strings if we do '10' + 5 we will get '105' and 10 + 5 will get us 15

8) boolean

9) true and false

10) == compears two values so it can make mistakes for instance it can assume that '10' ecauals 10 which is wrong but === compeares datatype too so '10'===10 returns false.

11) because == is less strict and it is preffered to know if datatypeyes are mathing or not.

12) != does not compeare datatype and !== does

13) && means and || means or ! means not

14) fales and true because and needs both to be true and or needs one to be true

15) !true prodiuces false

16) && can be used for example when we need someones age for example age to be more than 8 and less than 18 
and or can be  used for example gender for example if user puts in male or female both will be good.

17) because in javascript 0.1+0.2 is not exact 0.3 it continues and because of that it gives us false 

18) to round numbers that are way to long

19) const cant be changed and let can

20) it is for const values

